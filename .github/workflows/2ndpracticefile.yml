name: 2ndpractice github
on: push
jobs:
 test_expression:
  runs-on: ubuntu
  steps:
   - name: log expression
     run: echo "${{ toJSON(github.event) }}"
   - name: print message
     run: echo "hello world"
    
