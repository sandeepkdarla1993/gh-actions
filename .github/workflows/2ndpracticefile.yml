name: 2ndpractice github
on: 
 pull_request:
  types:
  - opened
 push:
  branches:
   - main
jobs:
 test_expression:
  runs-on: ubuntu-latest
  steps:
   - name: log expression
     run: echo "${{ toJSON(github.event) }}"
   - name: print message
     run: echo "hello world"
    
